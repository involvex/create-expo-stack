import * as Sentry from '@sentry/react-native';

Sentry.init({
  dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,
  // Consider uncommenting the line below for a better dev experience with fewer noiseless logs.
  // debug: __DEV__,
});
